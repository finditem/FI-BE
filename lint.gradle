apply plugin: 'com.diffplug.spotless'

spotless {
	enforceCheck false

	java {
		target 'src/*/java/**/*.java'
		palantirJavaFormat()
		formatAnnotations()
		removeUnusedImports()
		trimTrailingWhitespace()
		endWithNewline()
	}
}
